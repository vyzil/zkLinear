fn main() {
    eprintln!("developer-only stub binary");
    eprintln!("canonical e2e entrypoint: `spark_e2e_cli`");
    eprintln!(
        "example: cargo run --bin spark_e2e_cli -- compile <instance_dir> <compiled.json> m61"
    );
}
